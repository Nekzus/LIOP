---
title: "Inicio Rápido"
description: "Comienza con LIOP en menos de 5 minutos usando el SDK de TypeScript"
---

El ecosistema `@nekzus/liop` está diseñado para ser potente pero familiar. Si has construido servidores para MCP, la transición a LIOP es casi directa.

En esta guía, crearemos un Servidor LIOP que expone una herramienta y un Cliente que la invoca.

<Note>
  Internamente, LIOP compila tu lógica a WebAssembly y la inyecta vía P2P. El SDK abstrae todo esto para que te enfoques en la lógica de negocio.
</Note>

## 1. Instalación

Instala el SDK en tu proyecto de Node.js o TypeScript.

<CodeGroup>
```bash npm
npm install @nekzus/liop zod
```
```bash pnpm
pnpm add @nekzus/liop zod
```
```bash yarn
yarn add @nekzus/liop zod
```
</CodeGroup>

## 2. Crear un Servidor LIOP (Data Node)

Un Servidor LIOP actúa como un Nodo de Datos. Se conecta a la malla y espera lógica inyectada de agentes remotos.

Crea un archivo llamado `server.ts`:

```typescript server.ts
import { LiopServer } from '@nekzus/liop';
import { z } from 'zod';

// Inicializa el servidor con identidad
const server = new LiopServer({
  name: "MyLocalDataNode",
  version: "1.0.0"
});

// Expone una capacidad (Tool)
server.tool(
  "query_database",
  "Analiza tablas masivas directamente en el origen.",
  { query: z.string() },
  async ({ query }) => {
    console.log(`[Server] Ejecutando Logic-Injection-on-Origin: ${query}`);
    
    // Aquí se procesarían los datos locales (ej. 5GB)
    // Solo el resultado final sale del servidor
    return {
      content: [{ type: "text", text: `Resultado de la consulta: ${query}` }]
    };
  }
);

// Inicia la conexión y anúncialo en la malla DHT Kademlia
async function main() {
  await server.connect();
  console.log("Servidor LIOP escuchando en la malla...");
}

main().catch(console.error);
```

## 3. Crear un Cliente LIOP (Agent Node)

El Cliente actúa como la Inteligencia. Descubre servidores e inyecta solicitudes (intenciones).

Crea un archivo llamado `client.ts`:

```typescript client.ts
import { LiopClient } from '@nekzus/liop';

async function main() {
  const client = new LiopClient({
    name: "AI_Research_Agent",
    version: "2.5.1"
  });

  await client.connect();
  console.log("Conectado al Logic-Injection-on-Origin Protocol...");

  try {
    // Descubre el servidor e inyecta la lógica
    const result = await client.callTool("MyLocalDataNode", "query_database", {
      query: "SELECT * FROM massive_table WHERE anomaly = true"
    });

    console.log("Resultado verificado matemáticamente:");
    console.log(result.content[0].text);
  } catch (err) {
    console.error("Fallo en la ejecución del Agente:", err);
  }
}

main().catch(console.error);
```

## 4. Ejecutar la Malla

Primero, inicia el servidor:
```bash
npx tsx server.ts
# Salida: Servidor LIOP escuchando en la malla...
```

En otra terminal, lanza el Agente:
```bash
npx tsx client.ts
# Salida: Conectado al Logic-Injection-on-Origin Protocol...
# Salida: Resultado verificado matemáticamente:
# Salida: Resultado de la consulta: SELECT * FROM massive_table WHERE anomaly = true
```

## ¿Qué acaba de ocurrir?

A diferencia de las APIs tradicionales:
1. Tu Cliente descubrió el Servidor instantáneamente mediante **Kademlia P2P**.
2. Un handshake **Kyber768 Post-Cuántico** aseguró la comunicación.
3. La consulta se serializó en un formato **binario Protobuf** ultra compacto.
4. La ejecución ocurrió dentro de un **sandbox WASI** en el servidor, aislado del host físico.

## Siguientes Pasos

Ahora que has ejecutado tu primera misión en la malla, explora la arquitectura para aprovechar Watchdogs y Pruebas ZK.

<Card title="Arquitectura del Core" icon="sitemap" href="/es/concepts/architecture">
  Aprende cómo la capa de datos y transporte se sinergizan en LIOP.
</Card>
