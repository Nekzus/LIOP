[package]
name = "liop-core"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.12"
prost = "0.13"
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"

[build-dependencies]
protoc-bin-vendored = "3.2.0"
tonic-build = "0.12"

